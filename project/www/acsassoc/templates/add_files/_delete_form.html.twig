<form method="post" action="{{ path('app_add_files_delete', {'id': add_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ add_file.id) }}">
    <button class="btn"><img class="img-mod" src="{{ asset('img/poubelle.svg')}}" alt="supprimer" /></button>
</form>
