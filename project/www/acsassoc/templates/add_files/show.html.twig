{% extends 'base.html.twig' %}

{% block title %}AddFiles{% endblock %}

{% block body %}
    <h1>AddFiles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ add_file.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ add_file.name }}</td>
            </tr>
            <tr>
                <th>Src</th>
                <td>{{ add_file.src }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_add_files_index') }}">back to list</a>

    <a href="{{ path('app_add_files_edit', {'id': add_file.id}) }}">edit</a>

    {{ include('add_files/_delete_form.html.twig') }}
{% endblock %}
