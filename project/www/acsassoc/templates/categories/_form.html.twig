{{ form_start(form) }}
<div class="form-group row">
  <label class="col-sm-2 col-form-label">Nom</label>
    <div class="col-sm-10">

    {{form_row(form.name)}}
 
    </div>
</div>
    
    <div class="form-group row">
  <label class="col-sm-2 col-form-label">Couleur</label>
<div class="col-sm-10">
    {{form_row(form.color)}}
</div>
</div>
    
{% if button_label|default('Enregistrer') == 'Update' %}
<button class="btn bg-light bg-gradient"><img class="img-mod imd-delet-prd" src="{{ asset('img/available_updates.svg')}}" alt="update" /></button>
{% else %}
<button class="btn bg-light bg-gradient"><img class="img-mod imd-delet-prd" src="{{ asset('img/floppy.svg')}}" alt="enregistrer" /></button>
{% endif %}
{{ form_end(form) }}
