{% extends 'users.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <section class="container">
        <h1 class="text-center">Catégories</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ category.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ category.name }}</td>
                </tr>
                <tr>
                    <th>Slug</th>
                    <td>{{ category.slug }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn bg-light bg-gradient" href="{{ path('app_categories_index') }}"><img class="img-mod" src="{{ asset('img/revenir.svg')}}" alt="retour" /></a>

        <a class="btn bg-light bg-gradient" href="{{ path('app_categories_edit', {'id': category.id}) }}"><img class="img-mod" src="{{ asset('img/crayon.svg')}}" alt="modifier" /></a>

        {{ include('categories/_delete_form.html.twig') }}
    </sction>
{% endblock %}
